import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Let's see what happens, how we change the world with 3D printing
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(

  <React.StrictMode>
    <App />
  </React.StrictMode>
);


